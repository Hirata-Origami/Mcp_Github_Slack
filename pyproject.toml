[project]
name = "demo-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pygithub",
    "slack_sdk",
    "mcp[cli]>=1.6.0",
    "aiohttp>=3.9.0",
]
